<?php

	if ( $block->isValidAuth () && $block->isValidZone () ) {
		/* @noEscape */
		echo $block->renderSections ( "overview", array (
			"configuration",
			"status"
		));
	}
	else {
		/* @noEscape */
		echo $block->renderSections ( "overview", array (
			"configuration"
		));
	}

?>
